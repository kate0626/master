# 最適パラメータの説明（VLDB, start=0, 容量100, RW=100補正）

## 結論：最適構成

| パラメータ | 最適値 | 役割 |
|---|---|---|
| theta (θ) | **2** | 構造prior の強さ（θ·w_prior） |
| gamma (γ) | **0** | 次数指数（deg^γ）→ **0=次数を無視** |
| beta (β) | **1** | 距離指数（(1−α)^(β·hop)） |
| delta (δ) | 1 | アクセス回数の1-hit割引（max(0, freq−δ)） |
| lambda (λ) | 1 | 学習係数（効果なし＝既定でよい） |
| lw | 0 | 学習walk数（学習なしが最良） |

スコア式：`H = L + ( max(0,freq−1) + 2·(1−α)^hop + 1·learn ) / size`
（γ=0 で deg が消え、prior は純距離 (1−α)^hop になる）

**性能：hit 0.231 / per-start 45.8s（RW=100換算）。**

## 各パラメータの根拠（推移グラフ fig_a より）

### theta：1以上が必須（最重要レバー）
- θ=0 → hit **0.023**（崩壊）／θ=1,2,3 → **0.23**でほぼ横ばい
- 構造priorを完全に切る(θ=0)とキャッシュが優先順位を付けられず壊れる。θ≥1で頭打ち。θ=2が僅差で最良＝堅い

### gamma：0が最良（次数は無視すべき）
- γ=0 → **0.231** ／ γ=1 → 0.223 ／ γ=3 → **0.091**（崩壊）
- 次数を重視するほど悪化 → **「次数は再利用予測に無情報」を実証**（AUC~0.55 の知見と整合）
- 最良の prior は **距離だけ（deg^0=1）**

### beta：1が最良（距離の効かせすぎはNG）
- β=1 → 0.231 ／ β=0(距離無視) → 0.228 ／ β=3(急減衰) → 0.218
- 距離は適度に効かせる。急にしすぎる(β=3)と遠いが有用なエントリを切り捨て悪化

### delta：振っても平坦（δ=1が僅差で最良）
- δ=0 → 0.230 ／ δ=0.5 → 0.230 ／ δ=1 → **0.231** ／ δ=2 → 0.231
- 0〜2 でほぼ横ばい。1-hit割引の有無は hit をほとんど動かさない（この設定では one-hit が構造prior側で既に弾かれている）。δ=1 を採用（慣例・僅差最良）

### lambda / lw：効かない or 既定でよい
- λ：0.5/1/2 で hit ほぼ不変 → 学習係数は効果なし
- lw：学習ありは hit を上げず、walk が増えて時間だけ増（RW補正後は同等）。**学習なし(lw=0)が最も安い**
  - 参考：lw=50 で hit 0.232–0.233 と僅かに上がる場合もあるが、追加walkコストに見合わない

## 解釈（研究上の含意）
最適 prior が **「距離のみ・次数なし」(γ=0)** に収束した点が核心。
PPR局所近似のうち **近接性(距離)は有効だが、次数バイアスは逆効果**であることを、
パラメータ探索が裏付けた。一方で学習係数(λ)・学習フェーズ(lw)は本データでは寄与せず。

## 妥当性の限界
start=0 の1始点での最適化。最適構成を複数始点で再現確認すれば妥当性が完成する。
